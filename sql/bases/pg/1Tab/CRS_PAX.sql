CREATE TABLE CRS_PAX (
    BAG_NORM SMALLINT,
    BAG_NORM_UNIT VARCHAR(2),
    BAG_POOL SMALLINT,
    CABIN_CLASS VARCHAR(1),
    ETICK_CLASS VARCHAR(1),
    ETICK_SUBCLASS VARCHAR(1),
    INF_ID INTEGER,
    LAST_OP TIMESTAMP(0) NOT NULL,
    NAME VARCHAR(64),
    ORIG_CLASS VARCHAR(1),
    ORIG_SUBCLASS VARCHAR(1),
    PAX_ID INTEGER NOT NULL,
    PERS_TYPE VARCHAR(2) NOT NULL,
    PNR_ID INTEGER NOT NULL,
    PR_DEL SMALLINT DEFAULT 0 NOT NULL,
    SEATS SMALLINT DEFAULT 1 NOT NULL,
    SEAT_REM VARCHAR(4),
    SEAT_TYPE VARCHAR(4),
    SEAT_XNAME VARCHAR(1),
    SEAT_YNAME VARCHAR(3),
    SURNAME VARCHAR(64) NOT NULL,
    SYNC_CHKD SMALLINT NOT NULL,
    TID INTEGER NOT NULL,
    UNIQUE_REFERENCE VARCHAR(25)
);

