CREATE TABLE ARX_TLG_STAT (
    AIRLINE VARCHAR(3),
    AIRLINE_MARK VARCHAR(3),
    AIRP_DEP VARCHAR(3),
    EXTRA VARCHAR(50) NOT NULL,
    FLT_NO INTEGER,
    PART_KEY TIMESTAMP(0) NOT NULL,
    QUEUE_TLG_ID INTEGER NOT NULL,
    RECEIVER_CANON_NAME VARCHAR(6) NOT NULL,
    RECEIVER_COUNTRY VARCHAR(2),
    RECEIVER_DESCR VARCHAR(50) NOT NULL,
    RECEIVER_SITA_ADDR VARCHAR(7) NOT NULL,
    SCD_LOCAL_DATE TIMESTAMP(0),
    SENDER_CANON_NAME VARCHAR(6) NOT NULL,
    SENDER_COUNTRY VARCHAR(2),
    SENDER_DESCR VARCHAR(50) NOT NULL,
    SENDER_SITA_ADDR VARCHAR(7) NOT NULL,
    SUFFIX VARCHAR(1),
    TIME_CREATE TIMESTAMP(0) NOT NULL,
    TIME_RECEIVE TIMESTAMP(0),
    TIME_SEND TIMESTAMP(0),
    TLG_LEN INTEGER NOT NULL,
    TLG_TYPE VARCHAR(10) NOT NULL,
    TYPEB_TLG_ID INTEGER NOT NULL,
    TYPEB_TLG_NUM INTEGER NOT NULL
);

