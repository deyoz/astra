CREATE TABLE RFISC_SETS (
    AIRLINE VARCHAR(3) NOT NULL,
    AIRP_DEP VARCHAR(3),
    AUTO_CHECKIN SMALLINT NOT NULL,
    FLT_NO INTEGER,
    ID INTEGER NOT NULL,
    RFIC VARCHAR(1) NOT NULL,
    RFISC VARCHAR(15)
);

