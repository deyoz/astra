CREATE TABLE REMOTE_RESULTS (
    DATE_CR TIMESTAMP(0) NOT NULL,
    EDIERRCODE VARCHAR(3),
    EDISESSION_ID BIGINT NOT NULL,
    INTMSGID VARCHAR(24),
    PULT VARCHAR(6),
    RAWDATA BYTEA,
    REMARK VARCHAR(140),
    REMOTE_ID BIGINT NOT NULL,
    STATUS VARCHAR(2)
);

