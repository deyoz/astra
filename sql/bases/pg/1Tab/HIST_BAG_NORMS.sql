CREATE TABLE HIST_BAG_NORMS (
    AIRLINE VARCHAR(3),
    AMOUNT SMALLINT,
    BAG_TYPE SMALLINT,
    CITY_ARV VARCHAR(3),
    CITY_DEP VARCHAR(3),
    CLASS VARCHAR(1),
    CRAFT VARCHAR(3),
    EXTRA VARCHAR(2000),
    FIRST_DATE TIMESTAMP(0) NOT NULL,
    FLT_NO INTEGER,
    HIST_ORDER INTEGER NOT NULL,
    HIST_TIME TIMESTAMP(0) NOT NULL,
    ID INTEGER NOT NULL,
    LAST_DATE TIMESTAMP(0),
    NORM_TYPE VARCHAR(5) NOT NULL,
    PAX_CAT VARCHAR(4),
    PER_UNIT SMALLINT,
    PR_DEL SMALLINT NOT NULL,
    PR_TRFER SMALLINT,
    SUBCLASS VARCHAR(1),
    TRIP_TYPE VARCHAR(1),
    WEIGHT SMALLINT
);

