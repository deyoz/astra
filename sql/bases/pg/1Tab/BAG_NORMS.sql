CREATE TABLE BAG_NORMS (
    AIRLINE VARCHAR(3),
    AMOUNT SMALLINT,
    BAG_TYPE SMALLINT,
    CITY_ARV VARCHAR(3),
    CITY_DEP VARCHAR(3),
    CLASS VARCHAR(1),
    CRAFT VARCHAR(3),
    DIRECT_ACTION SMALLINT NOT NULL,
    EXTRA VARCHAR(2000),
    FIRST_DATE TIMESTAMP(0) NOT NULL,
    FLT_NO INTEGER,
    ID INTEGER NOT NULL,
    LAST_DATE TIMESTAMP(0),
    NORM_TYPE VARCHAR(5) NOT NULL,
    PAX_CAT VARCHAR(4),
    PER_UNIT SMALLINT,
    PR_DEL SMALLINT NOT NULL,
    PR_TRFER SMALLINT,
    SUBCLASS VARCHAR(1),
    TID INTEGER NOT NULL,
    TRIP_TYPE VARCHAR(1),
    WEIGHT SMALLINT
);

