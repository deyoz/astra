CREATE TABLE ARX_BAG_NORMS (
AIRLINE VARCHAR(3),
AMOUNT INT,
BAG_TYPE INT,
CITY_ARV VARCHAR(3),
CITY_DEP VARCHAR(3),
CLASS VARCHAR(1),
CRAFT VARCHAR(3),
DIRECT_ACTION INT NOT NULL,
EXTRA VARCHAR(2000),
FIRST_DATE TIMESTAMP NOT NULL,
FLT_NO INT,
ID INT NOT NULL,
LAST_DATE TIMESTAMP,
NORM_TYPE VARCHAR(5) NOT NULL,
PART_KEY TIMESTAMP NOT NULL,
PAX_CAT VARCHAR(4),
PER_UNIT INT,
PR_DEL INT NOT NULL,
PR_TRFER INT,
SUBCLASS VARCHAR(1),
TID INT NOT NULL,
TRIP_TYPE VARCHAR(1),
WEIGHT INT
);
