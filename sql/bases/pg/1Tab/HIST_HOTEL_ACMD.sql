CREATE TABLE HIST_HOTEL_ACMD (
    AIRLINE VARCHAR(3),
    AIRP VARCHAR(3) NOT NULL,
    DOUBLE_AMOUNT INTEGER NOT NULL,
    HIST_ORDER INTEGER NOT NULL,
    HIST_TIME TIMESTAMP(0) NOT NULL,
    HOTEL_NAME VARCHAR(100) NOT NULL,
    ID INTEGER NOT NULL,
    SINGLE_AMOUNT INTEGER NOT NULL
);

