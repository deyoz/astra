CREATE TABLE PAX_GRP (
    AIRP_ARV VARCHAR(3) NOT NULL,
    AIRP_DEP VARCHAR(3) NOT NULL,
    BAG_REFUSE SMALLINT NOT NULL,
    CLASS VARCHAR(1),
    CLASS_GRP INTEGER,
    CLIENT_TYPE VARCHAR(5) NOT NULL,
    DESK VARCHAR(6),
    EXCESS_PC SMALLINT NOT NULL,
    EXCESS_WT SMALLINT NOT NULL,
    GRP_ID INTEGER NOT NULL,
    HALL INTEGER,
    INBOUND_CONFIRM SMALLINT NOT NULL,
    PIECE_CONCEPT SMALLINT,
    POINT_ARV INTEGER NOT NULL,
    POINT_DEP INTEGER NOT NULL,
    POINT_ID_MARK INTEGER NOT NULL,
    PR_MARK_NORMS SMALLINT NOT NULL,
    ROLLBACK_GUARANTEED SMALLINT,
    STATUS VARCHAR(1) NOT NULL,
    TID INTEGER NOT NULL,
    TIME_CREATE TIMESTAMP(0),
    TRFER_CONFIRM SMALLINT NOT NULL,
    TRFER_CONFLICT SMALLINT NOT NULL,
    USER_ID INTEGER
);

