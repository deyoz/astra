CREATE TABLE TRIP_COMP_RATES (
    COLOR VARCHAR(10) NOT NULL,
    NUM SMALLINT NOT NULL,
    POINT_ID INTEGER NOT NULL,
    RATE DECIMAL(12,2) NOT NULL,
    RATE_CUR VARCHAR(3) NOT NULL,
    X SMALLINT NOT NULL,
    Y SMALLINT NOT NULL
);

