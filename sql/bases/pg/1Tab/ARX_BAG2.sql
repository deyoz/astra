CREATE TABLE ARX_BAG2 (
    AIRLINE VARCHAR(3),
    AMOUNT SMALLINT DEFAULT 0 NOT NULL,
    BAG_POOL_NUM SMALLINT NOT NULL,
    BAG_TYPE SMALLINT,
    BAG_TYPE_STR VARCHAR(15),
    DESK VARCHAR(6),
    GRP_ID INTEGER NOT NULL,
    HALL INTEGER,
    HANDMADE SMALLINT,
    ID INTEGER NOT NULL,
    IS_TRFER SMALLINT,
    LIST_ID INTEGER,
    NUM SMALLINT NOT NULL,
    PART_KEY TIMESTAMP(0) NOT NULL,
    PR_CABIN SMALLINT NOT NULL,
    PR_LIAB_LIMIT SMALLINT NOT NULL,
    RFISC VARCHAR(15),
    SERVICE_TYPE VARCHAR(1),
    TIME_CREATE TIMESTAMP(0),
    TO_RAMP SMALLINT,
    USER_ID INTEGER,
    USING_SCALES SMALLINT,
    VALUE_BAG_NUM SMALLINT,
    WEIGHT SMALLINT DEFAULT 0 NOT NULL
);

