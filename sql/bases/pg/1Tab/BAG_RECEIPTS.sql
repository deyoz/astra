CREATE TABLE BAG_RECEIPTS (
    AIRCODE VARCHAR(3) NOT NULL,
    AIRLINE VARCHAR(3) NOT NULL,
    AIRP_ARV VARCHAR(3) NOT NULL,
    AIRP_DEP VARCHAR(3) NOT NULL,
    ANNUL_DATE TIMESTAMP(0),
    ANNUL_DESK VARCHAR(6),
    ANNUL_USER_ID INTEGER,
    BAG_NAME VARCHAR(50),
    BAG_TYPE SMALLINT,
    DESK_LANG VARCHAR(2) NOT NULL,
    EXCH_PAY_RATE DECIMAL(10,4),
    EXCH_RATE INTEGER,
    EX_AMOUNT SMALLINT,
    EX_WEIGHT SMALLINT,
    FLT_NO INTEGER,
    FORM_TYPE VARCHAR(7) NOT NULL,
    GRP_ID INTEGER,
    ISSUE_DATE TIMESTAMP(0) NOT NULL,
    ISSUE_DESK VARCHAR(6) NOT NULL,
    ISSUE_PLACE VARCHAR(100) NOT NULL,
    ISSUE_USER_ID INTEGER NOT NULL,
    IS_INTER SMALLINT NOT NULL,
    KIT_ID INTEGER,
    KIT_NUM SMALLINT,
    NDS DECIMAL(12,2),
    NDS_CUR VARCHAR(3),
    NO BIGINT NOT NULL,
    PAX_DOC VARCHAR(50),
    PAX_NAME VARCHAR(50),
    PAY_RATE_CUR VARCHAR(3) NOT NULL,
    POINT_ID INTEGER NOT NULL,
    PREV_NO VARCHAR(50),
    RATE DECIMAL(12,2) NOT NULL,
    RATE_CUR VARCHAR(3) NOT NULL,
    RECEIPT_ID INTEGER NOT NULL,
    REMARKS VARCHAR(50),
    SCD_LOCAL_DATE TIMESTAMP(0),
    SERVICE_TYPE SMALLINT NOT NULL,
    STATUS VARCHAR(1) NOT NULL,
    SUFFIX VARCHAR(1),
    TICKETS VARCHAR(50) NOT NULL,
    VALUE_TAX DECIMAL(4,1)
);

