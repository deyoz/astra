CREATE TABLE ARX_RFISC_STAT (
    AIRP_ARV VARCHAR(3) NOT NULL,
    COUPON_NO SMALLINT,
    DESK VARCHAR(6),
    EXCESS SMALLINT,
    FQT_NO VARCHAR(50),
    PAID SMALLINT,
    PART_KEY TIMESTAMP(0) NOT NULL,
    POINT_ID INTEGER NOT NULL,
    POINT_NUM INTEGER NOT NULL,
    PR_TRFER SMALLINT NOT NULL,
    REG_NO SMALLINT,
    RFISC VARCHAR(15) NOT NULL,
    TAG_NO BIGINT NOT NULL,
    TICKET_NO VARCHAR(15),
    TIME_CREATE TIMESTAMP(0),
    TRAVEL_TIME TIMESTAMP(0),
    TRFER_AIRLINE VARCHAR(3),
    TRFER_AIRP_ARV VARCHAR(3),
    TRFER_FLT_NO INTEGER,
    TRFER_SCD TIMESTAMP(0),
    TRFER_SUFFIX VARCHAR(1),
    USER_DESCR VARCHAR(20),
    USER_LOGIN VARCHAR(20)
);

