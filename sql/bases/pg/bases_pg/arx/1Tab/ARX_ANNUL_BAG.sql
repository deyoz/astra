CREATE TABLE ARX_ANNUL_BAG (
    AIRLINE VARCHAR(3),
    AIRP_ARV VARCHAR(3),
    AIRP_DEP VARCHAR(3),
    AMOUNT SMALLINT,
    BAG_TYPE SMALLINT,
    FLT_NO INTEGER,
    GRP_ID INTEGER,
    ID INTEGER,
    NAME VARCHAR(64),
    PART_KEY TIMESTAMP(0) NOT NULL,
    PAX_ID INTEGER,
    RFISC VARCHAR(15),
    SCD_OUT TIMESTAMP(0),
    SUFFIX VARCHAR(1),
    SURNAME VARCHAR(64),
    TIME_ANNUL TIMESTAMP(0),
    TIME_CREATE TIMESTAMP(0),
    TRFER_AIRLINE VARCHAR(3),
    TRFER_FLT_NO INTEGER,
    TRFER_SCD_OUT TIMESTAMP(0),
    TRFER_SUFFIX VARCHAR(1),
    USER_ID INTEGER,
    WEIGHT SMALLINT
);

