stages:
  - build
  
variables:
  GIT_CLONE_PATH: $CI_BUILDS_DIR/stand  

cache:
  key: "$CI_COMMIT_REF_SLUG"
  paths:
    - externallibs/

build:
  stage: build

  script:
    ./buildFromScratch.sh astra_stand/stand
